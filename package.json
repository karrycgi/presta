{
  "name": "mypresta",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf data",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "copy:pay": "cp -R demopay data/ps/modules"
  },
  "dependencies": {
    "rimraf": "^6.0.1"
  }
}